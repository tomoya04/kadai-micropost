<% if users.any? %>
  
<% users.each do |user| %>
 <img class="img-rounded" src = "<%= gravatar_url(user, {size: 50 }) %>" alt="">
 
 <div>
 <%= user.name %>
 </div>
 <div>
   <p><%= link_to "プロフィール", user_path(user) %></p>
 </div>
 
<% end %>
  
  <%= paginate users %>
<% end %>
